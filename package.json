{
  "name": "chess-mono",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "start": "cd ./apps/user-app && npm run dev",
    "db:generate": "cd packages/db && npx prisma generate",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "prettier": "^3.2.5",
    "prisma": "^6.2.1",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "cors": "^2.8.5"
  }
}
